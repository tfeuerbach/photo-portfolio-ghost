# Environment files (but track the example)
.env
!env.example

# SSL certificates and certbot data (legacy)
ssl/
certbot/

# Cloudflare Tunnel credentials (secret)
cloudflared/credentials.json

# Logs
nginx/logs/
*.log

# Backups
backups/
manual/
manual.tar.gz

# Database data
db_data/

# Ghost uploaded images (not tracked, but backed up separately)
ghost/content/images/
ghost/content/themes/casper-modified/
ghost/content/themes/casper
ghost/content/themes/source

# Temporary files
*.tmp
*.swp
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Docker
.docker/

# Node modules (if any)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*